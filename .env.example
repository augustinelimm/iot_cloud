# Copy this file to .env.local and fill in your actual values
# Note: .env.local should be added to .gitignore to keep your keys secure

# API endpoint for the IoT washer backend
VITE_API_ENDPOINT=https://api.iotwasher.com

# Readings API endpoint (use /api/readings for local dev with proxy)
# For production, set this to your proxied API endpoint
VITE_READINGS_API=/api/readings
